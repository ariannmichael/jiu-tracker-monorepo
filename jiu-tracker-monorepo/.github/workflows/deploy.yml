name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
        env:
          REGISTRY: ghcr.io

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: jiu-tracker-nest/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/${{ github.repository }}/backend:latest
            ghcr.io/${{ github.repository }}/backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Add deployment steps (e.g. deploy to Railway, Fly.io, ECS) using the built image.
  # Configure secrets (e.g. REGISTRY, DEPLOY_TOKEN) in repo settings.
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to target
  #       run: |
  #         echo "Configure your deploy step (e.g. kubectl, railway up, fly deploy)"
